<%
  # Récupérer les données du coefficient
  coefficient_data = @result.product_kits
  coefficient_total = coefficient_data['coefficient_total'].to_f
  base_coefficient = coefficient_data['base'].to_f
  details = coefficient_data['details'] || []
  points_cumules = coefficient_total - base_coefficient
%>

<div class="min-h-screen bg-gray-50" data-controller="clipboard">
  <!-- Header avec logo Cogeli -->
  <div class="bg-white shadow-sm border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <div class="flex items-center justify-between">
        <div class="flex items-center space-x-3">
          <div class="h-10 w-10 bg-gray-800 rounded-lg flex items-center justify-center">
            <span class="text-white font-bold text-lg">C</span>
          </div>
          <h1 class="text-2xl font-bold text-gray-900">Coefficient de Pose Calculé</h1>
        </div>
        <div class="text-sm text-gray-500">
          Session: <%= @current_session.session_token.first(8) %>
        </div>
      </div>
    </div>
  </div>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

    <!-- Coefficient Total en grand -->
    <div class="bg-gradient-to-r from-green-500 to-green-600 rounded-2xl p-8 text-white text-center mb-8 shadow-lg">
      <p class="text-lg mb-2 opacity-90">Coefficient Total de Pose</p>
      <p class="text-6xl font-bold mb-4">
        <%= coefficient_total %>
      </p>
      <div class="flex justify-center items-center space-x-8 text-sm">
        <div class="bg-white bg-opacity-20 rounded-lg px-4 py-2">
          <span class="block font-semibold">Base</span>
          <span class="text-lg"><%= base_coefficient %></span>
        </div>
        <div class="text-2xl">+</div>
        <div class="bg-white bg-opacity-20 rounded-lg px-4 py-2">
          <span class="block font-semibold">Points cumulés</span>
          <span class="text-lg"><%= points_cumules.round(2) %></span>
        </div>
      </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

      <!-- Colonne principale - Détail du calcul -->
      <div class="lg:col-span-2 space-y-6">

        <!-- Récapitulatif des réponses -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
          <h2 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
            <svg class="w-5 h-5 mr-2 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            Configuration sélectionnée
          </h2>
          <div class="space-y-3">
            <% @current_session.user_responses.includes(:question).each do |response| %>
              <div class="flex justify-between items-center py-2 border-b border-gray-100 last:border-b-0">
                <span class="text-sm font-medium text-gray-700"><%= response.question.title %></span>
                <span class="text-sm text-gray-900 bg-gray-50 px-2 py-1 rounded"><%= response.answer %></span>
              </div>
            <% end %>
          </div>
        </div>

        <!-- Détail des points -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
          <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
            <svg class="w-5 h-5 mr-2 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
            </svg>
            Détail du calcul
          </h3>

          <div class="space-y-2">
            <!-- Base -->
            <div class="flex justify-between py-2 border-b border-gray-200">
              <span class="text-gray-600">Coefficient de base</span>
              <span class="font-mono font-semibold text-gray-900">
                <%= base_coefficient %>
              </span>
            </div>

            <!-- Détails des points -->
            <% details.each do |detail| %>
              <div class="flex justify-between py-2 border-b border-gray-200">
                <div class="flex-1">
                  <span class="text-gray-600 text-sm"><%= detail['question'] %></span>
                  <span class="text-xs text-gray-500 ml-2 block">(<%= detail['answer'] %>)</span>
                </div>
                <span class="font-mono text-green-600 font-semibold">
                  +<%= detail['points'] %>
                </span>
              </div>
            <% end %>

            <!-- Total -->
            <div class="flex justify-between py-3 mt-4 border-t-2 border-gray-300 bg-gray-50 rounded-lg px-3">
              <span class="text-lg font-semibold text-gray-900">Total</span>
              <span class="text-lg font-mono font-bold text-green-600">
                <%= coefficient_total %>
              </span>
            </div>
          </div>
        </div>

        <!-- Interprétation -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
          <h3 class="text-lg font-semibold text-gray-900 mb-3 flex items-center">
            <svg class="w-5 h-5 mr-2 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            Interprétation du coefficient
          </h3>

          <% if coefficient_total <= 6 %>
            <div class="p-4 bg-green-50 border border-green-200 rounded-lg">
              <h4 class="font-semibold text-green-800 mb-2">🟢 Installation Simple</h4>
              <p class="text-green-700 text-sm">
                Votre installation est relativement basique et ne devrait pas présenter de difficultés particulières.
                <strong>Temps estimé : 1/2 journée.</strong>
              </p>
            </div>
          <% elsif coefficient_total <= 10 %>
            <div class="p-4 bg-yellow-50 border border-yellow-200 rounded-lg">
              <h4 class="font-semibold text-yellow-800 mb-2">🟡 Installation Standard</h4>
              <p class="text-yellow-700 text-sm">
                Votre installation présente une complexité moyenne avec quelques éléments techniques à prendre en compte.
                <strong>Temps estimé : 1 journée.</strong>
              </p>
            </div>
          <% elsif coefficient_total <= 15 %>
            <div class="p-4 bg-orange-50 border border-orange-200 rounded-lg">
              <h4 class="font-semibold text-orange-800 mb-2">🟠 Installation Complexe</h4>
              <p class="text-orange-700 text-sm">
                Votre installation nécessite une expertise technique approfondie et plusieurs interventions spécifiques.
                <strong>Temps estimé : 1 à 2 journées.</strong>
              </p>
            </div>
          <% else %>
            <div class="p-4 bg-red-50 border border-red-200 rounded-lg">
              <h4 class="font-semibold text-red-800 mb-2">🔴 Installation Très Complexe</h4>
              <p class="text-red-700 text-sm">
                Votre installation est particulièrement technique et nécessitera une équipe expérimentée et du temps supplémentaire.
                <strong>Temps estimé : 2 journées ou plus.</strong>
              </p>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Sidebar - Actions -->
      <div class="lg:col-span-1">
        <div class="sticky top-8 space-y-6">

          <!-- Résumé coefficient -->
          <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
              <svg class="w-5 h-5 mr-2 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
              </svg>
              Résumé
            </h3>

            <div class="text-center">
              <div class="text-4xl font-bold text-gray-900 mb-2">
                <%= coefficient_total %>
              </div>
              <p class="text-sm text-gray-600">Coefficient total</p>

              <div class="mt-4 text-xs text-gray-500 space-y-1">
                <div>Base: <%= base_coefficient %></div>
                <div>Points: +<%= points_cumules.round(2) %></div>
                <div><%= details.count %> critères évalués</div>
              </div>
            </div>
          </div>

          <!-- Actions -->
          <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-4">Actions</h3>
            <div class="space-y-3">
              <a href="<%= root_path %>"
                 class="w-full bg-gray-800 hover:bg-gray-900 text-white font-medium py-3 px-4 rounded-lg transition-colors flex items-center justify-center">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                </svg>
                Nouveau calcul
              </a>

              <button data-action="click->clipboard#print"
                      class="w-full bg-white hover:bg-gray-50 text-gray-700 font-medium py-3 px-4 rounded-lg border border-gray-300 transition-colors flex items-center justify-center">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z"></path>
                </svg>
                Imprimer
              </button>

              <button data-action="click->clipboard#copyCoefficient"
                      data-coefficient="<%= coefficient_total %>"
                      class="w-full bg-white hover:bg-gray-50 text-gray-700 font-medium py-3 px-4 rounded-lg border border-gray-300 transition-colors flex items-center justify-center">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
                </svg>
                Copier coefficient
              </button>
            </div>
          </div>

          <!-- Info contact -->
          <div class="bg-gray-800 rounded-xl p-6 text-white">
            <h3 class="text-lg font-semibold mb-4">Besoin d'aide ?</h3>
            <p class="text-gray-300 text-sm mb-4">
              Nos experts Cogeli sont là pour vous accompagner dans votre projet d'installation.
            </p>
            <button class="w-full bg-white text-gray-800 font-medium py-2 px-4 rounded-lg hover:bg-gray-100 transition-colors">
              Contacter un expert
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

