<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    * { box-sizing: border-box; }
    body { font-family: 'Helvetica', sans-serif; margin: 0; padding: 0; }
    .header { background: #1e3a8a; color: white; padding: 30px; text-align: center; }
    .logo { font-size: 36px; font-weight: bold; margin-bottom: 10px; }
    .subtitle { font-size: 14px; opacity: 0.9; }
    .container { padding: 30px; }
    .section { margin-bottom: 30px; page-break-inside: avoid; }
    .section-title {
      font-size: 18px;
      font-weight: bold;
      color: #1e3a8a;
      border-bottom: 2px solid #1e3a8a;
      padding-bottom: 8px;
      margin-bottom: 15px;
    }
    table { width: 100%; border-collapse: collapse; }
    th { background: #f3f4f6; padding: 10px; text-align: left; font-weight: bold; }
    td { padding: 10px; border-bottom: 1px solid #e5e7eb; }
    .kit-code { font-weight: bold; color: #1e3a8a; }
    .price { text-align: right; font-weight: bold; color: #059669; }
    .total-section {
      background: #eff6ff;
      padding: 20px;
      margin-top: 30px;
      border: 2px solid #1e3a8a;
      border-radius: 8px;
    }
    .total-row { display: flex; justify-content: space-between; font-size: 24px; font-weight: bold; }
    .info-box { background: #f9fafb; padding: 15px; border-left: 4px solid #1e3a8a; margin: 20px 0; }
    .footer-info { font-size: 10px; color: #6b7280; text-align: center; margin-top: 40px; }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
